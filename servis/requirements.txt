# Веб-сервис. Лицензии выбраны так, чтобы продукт можно было отдавать
# сторонним организациям: ни одной AGPL-зависимости.
fastapi>=0.110          # MIT
uvicorn>=0.27           # BSD
jinja2>=3.1             # BSD
python-multipart>=0.0.9 # Apache 2.0  (загрузка файлов формой)
pdfplumber>=0.11        # MIT — текстовый слой PDF с координатами
pillow>=10.2            # MIT-CMU — подготовка картинок перед OCR
anthropic>=0.40         # MIT — вторая ступень распознавания сканов
